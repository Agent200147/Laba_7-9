{{ form_start(form_comment, { 'attr': {'class': 'form--loadComment--wrapper col-md-2 animate__animated animate__fadeIn'} }) }}
    {{ form_widget(form_comment.content, { 'attr': {'placeholder': 'Оставьте комментарий'}, 'label': ' ' }) }}
    <button type="submit" class="btn btn-primary">Отправить</button>

{{ form_end(form_comment) }}

