<div class="navbar animate__animated animate__fadeInDown animate__fast">
    <a href="/"><div class="logo"> AN{{ include('components/sfera.html') }}</div></a>
    <ul class="menu">
        <li>О нас</li>
        <li>Меню</li>
        <li>Выход</li>
    </ul>
    {% if app.user %}
        Привет, {{ app.user.login }}

        <div class="btns">

        {% if is_granted('ROLE_ADMIN') %}
            <a href="/admin" class="btn btn-md btn-outline-info">Админка</a>
        {% endif %}
            <a href="/addNew"><input type="button" name="zareg" class="btn btn-md btn-outline-secondary" value="Добавление новости"></a>

            <a href="/logout"><input type="button" name="zareg" class="btn btn-md btn-outline-danger" value="Выйти"></a>

        </div>
    {% else %}
    <div class="buttons">
        <a href="/register"><input type="button" name="zareg" class="reg" value="Зарегистрироваться"></a>
        <a href="/login"><input type="button" name="vhod" class="vhod" value="Войти"></a>
    </div>

    {% endif %}
</div>

<div class="h animate__animated animate__fadeInUp">Aesthetic News - последние новости на портале</div>
<div class="lineH col-md-11 animate__animated animate__fadeIn animate__delay-1s"></div>

{% if not app.user %}

{% endif %}